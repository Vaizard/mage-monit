check process sshd with pidfile /run/rsyslog.pid
  group system
  start program "/usr/sbin/service rsyslog start"
  stop program "/usr/sbin/service rsyslog stop"
  if 5 restarts within 5 cycles then timeout
